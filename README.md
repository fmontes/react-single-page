# Same as always...
1. ```npm install```
2. ```bower install```
3. ```gulp watch```


## Update the config file

````
/app/js/config/index.js
````


````javascript
export default {
    xUserId: '',
    parse: {
        appId: 'PARSE APP ID',
        jsKey: 'PARSE JAVASCRIPT KEY'
    }
}
````